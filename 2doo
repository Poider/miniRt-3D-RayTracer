brightness variable?
camera segfault 0,-4,0

init object transformation sphere->transformation (where is it initialized)


is transparency/refractive_index done by default?

matrices leaking when u multiply and such?